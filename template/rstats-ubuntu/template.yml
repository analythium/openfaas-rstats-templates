language: rstats-ubuntu
fprocess: Rscript index.R
welcome_message: |
  You have created a new function which uses R and the Classic
  OpenFaaS template.

  To include third-party dependencies, add packages, remotes,
  and system dependencies to the DESCRIPTION file.

  For high-throughput applications, we recommend using the
  HTTP-based rstats templates available via the store.
